
diff --git a/app/okrs.py b/app/okrs.py
--- a/app/okrs.py
+++ b/app/okrs.py
@@
-from ..modulobase import (
-    carregar_base_eshows,
-    carregar_eshows_excluidos,
-    carregar_base2,
-    carregar_ocorrencias,
-    carregar_base_inad,
-    carregar_metas,
-    carregar_pessoas
+from ..data_loader import (
+    get_eshows,
+    get_base2,
+    get_ocorrencias,
+    get_inad,
+    get_pessoas
 )
@@
-df_eshows = carregar_base_eshows()
-df_base2 = carregar_base2()
-df_ocorrencias = carregar_ocorrencias()
+df_eshows = get_eshows()
+df_base2 = get_base2()
+df_ocorrencias = get_ocorrencias()

diff --git a/app/kpis/kpis.py b/app/kpis/kpis.py
--- a/app/kpis/kpis.py
+++ b/app/kpis/kpis.py
@@
-from ..modulobase import (
-    carregar_base_eshows,
-    carregar_base2,
-    carregar_pessoas,
-    carregar_ocorrencias,
-    carregar_base_inad,
-    carregar_eshows_excluidos,
+from ..data_loader import (
+    get_eshows,
+    get_base2,
+    get_pessoas,
+    get_ocorrencias,
+    get_inad,
 )

